% ai, repomix

# Generate LLM bundle output file using repomix.
repomix \
  --style xml \
  --no-files \
  --ignore '**/uv.lock,**/package-lock.json,**/.env,**/Cargo.lock,**/node_modules,**/target,**/dist,**/build,**/output.txt,**/yarn.lock'

