% java, spring

# Change log level in spring-boot service at runtime
curl -s -X POST "http://localhost:9121/actuator/loggers/<logger>" -H "Content-Type: application/json" -d '{"configuredLevel":"<level>"}'

$ logger: echo -e 'com.example\n'
$ level: echo -e 'ERROR\nWARN\nINFO\nDEBUG'
